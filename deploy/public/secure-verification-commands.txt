# SECURE VERIFICATION COMMANDS
# Copy-paste these exact commands to verify everything manually

# 1. VERIFY FILE SIZES (You can run this yourself)
cd "/Users/l/Library/Mobile Documents/com~apple~CloudDocs/LD/LD APP/Recipes@Groceries/deploy/public"
wc -l app.js *-real.js

# 2. SEARCH FOR REDUNDANT METHODS IN APP.JS (You can verify)
grep -n "toggleShoppingItemComplete\|renderShoppingList\|addProduct\|deleteProduct" app.js

# 3. VERIFY JSON IMPORT/EXPORT SIZE CLAIM
wc -l json-import-export-real.js
# Should show 385 lines, NOT 2,500

# 4. CHECK IF METHODS EXIST IN BOTH PLACES (DUPLICATION)
echo "=== Shopping methods in app.js ==="
grep -n "toggleShoppingItemComplete" app.js
echo "=== Shopping methods in shopping-list-real.js ==="
grep -n "toggleComplete\|toggle.*complete" shopping-list-real.js

# 5. VERIFY BACKUP EXISTS BEFORE ANY CHANGES
ls -la app.js.backup-*

# 6. FIND EXACT METHOD LINES TO REMOVE
echo "=== Methods to potentially remove from app.js ==="
grep -n "^    toggleShoppingItemComplete\|^    renderShoppingList\|^    addProduct\|^    deleteProduct" app.js